<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<a class="navbar-brand" href="/">TP Gaming</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarSupportedContent">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item active">
				<a class="nav-link" href="/"">Accueil</a>
			</li>

			{% if is_granted('IS_AUTHENTICATED_FULLY') %}

				<li class="nav-item">
					<a class="nav-link" href={{path("app_game_new")}}>Ajouter un nouveau jeu</a>
				</li>

				<li class="nav-item">
					<a class="nav-link" href={{path("app_logout")}}>Se déconnecter</a>
				</li>
                <li class="nav-item">
					<a class="nav-link" href={{path("app_user_index")}}>Bienvenue {{ app.user.username}} !!</a>
				</li>
			
			{% if is_granted('ROLE_ADMIN') %}

				<li class="nav-item">
					<a class="nav-link" href={{path("admin")}}>Administration</a>
				</li>
			{% endif %}
			{% else %}
				<li class="nav-item">
					<a class="nav-link" href={{path("app_login")}}>Se connecter</a>
				</li>

				<li class="nav-item">
					<a class="nav-link" href={{path("app_register")}}>S'enregistrer</a>
				</li>
			{% endif %}


		</ul>


	</div>
</nav>
